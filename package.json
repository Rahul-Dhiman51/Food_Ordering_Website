{
  "name": "food_ordering_website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "prebuild": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "postbuild": "move /Y frontend/dist backend/src/public"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20 <21"
  }
}
